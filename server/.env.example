# Server Port (dynamically assigned by hosting platforms or defaults to 5000)
PORT=5000

# Allowed CORS Origin(s) (comma-separated for multiple origins, or * for all)
# Local development: http://localhost:8080
# Production: https://your-client-app.vercel.app,http://localhost:8080
CORS_ORIGIN=http://localhost:8080
